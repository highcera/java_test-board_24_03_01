import java.util.Arrays;
import java.util.Map;

class Util {
    static Map<String, String> getParms(String queryStr) {
        System.out.println(queryStr.split("\\?")[0]);
        System.out.println(queryStr.split("\\?")[1]);
        return null;
    }
}

